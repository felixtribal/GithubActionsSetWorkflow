name: Setup Node

description: Setup a Node.js environment.


runs:
  using: 'composite'
  steps:
    - name: Use Node.js 
      uses: actions/setup-node@v4
      with:
        node-version: {{ matrix.node-version }}
        cache: 'npm'